# Deprecated for shared deployments:
# Putting VITE_ keys in the frontend bundle means anyone can extract them.
# Prefer exporting an encrypted config file from the app and, if needed,
# placing it at public/default-api-config.json for password-protected import.
VITE_GEMINI_API_KEY="MY_LOCAL_ONLY_GEMINI_API_KEY"
