# Optional local-only fallback for Gemini native calls.
# Do NOT set this for public Cloudflare Pages deployments unless you accept
# exposing the key in the built frontend bundle.
VITE_GEMINI_API_KEY="MY_GEMINI_API_KEY"
